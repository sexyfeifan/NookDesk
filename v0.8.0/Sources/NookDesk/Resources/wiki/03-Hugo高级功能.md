# 03-高级功能

## TypeScript 文章管理

NookDesk 支持直接管理 `src/pages/Home/posts.ts` 中的文章数据，使用 TypeScript 格式存储。

## 页面编辑

通过「页面」标签页，可以直接编辑 Home.tsx 和 posts.ts 文件。

## 多语言内容工作区

如果项目配置了多个内容目录，可以在写作页顶部直接切换。

## 翻译副本

创建翻译副本时，软件会尽量保持原文的相对路径结构，并补齐 `translationKey`。

## AI 写作助手

配置 AI API 后，可以使用 AI 辅助生成和排版文章内容。
